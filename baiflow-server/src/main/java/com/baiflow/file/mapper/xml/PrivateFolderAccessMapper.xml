<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baiflow.file.mapper.PrivateFolderAccessMapper">

    <sql id="pfaColumns">
        id, user_id, file_item_id, access_token_hash, expires_at, created_at
    </sql>

    <select id="selectValidByUserAndFolder" resultType="com.baiflow.file.entity.PrivateFolderAccess">
        SELECT <include refid="pfaColumns"/>
        FROM private_folder_access
        WHERE user_id = #{userId}
          AND file_item_id = #{fileItemId}
          AND expires_at > NOW()
        ORDER BY created_at DESC
    </select>

    <delete id="deleteByFileItemId">
        DELETE FROM private_folder_access WHERE file_item_id = #{fileItemId}
    </delete>

    <delete id="deleteExpired">
        DELETE FROM private_folder_access WHERE expires_at &lt;= NOW()
    </delete>

</mapper>
