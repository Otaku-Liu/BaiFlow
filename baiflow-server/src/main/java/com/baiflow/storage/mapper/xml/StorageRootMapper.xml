<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baiflow.storage.mapper.StorageRootMapper">
    <select id="selectAllOrdered" resultType="com.baiflow.storage.entity.StorageRoot">
        SELECT id, name, type, root_path, status, readonly, created_at, updated_at
        FROM storage_root
        <where>
            <if test="status != null and status != ''">status = #{status}</if>
        </where>
        ORDER BY created_at DESC
    </select>

    <select id="selectByType" resultType="com.baiflow.storage.entity.StorageRoot">
        SELECT id, name, type, root_path, status, readonly, created_at, updated_at
        FROM storage_root
        WHERE type = #{type}
        ORDER BY created_at DESC
    </select>
</mapper>
