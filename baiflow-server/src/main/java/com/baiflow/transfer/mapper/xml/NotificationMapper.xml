<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baiflow.transfer.mapper.NotificationMapper">

    <sql id="notifColumns">
        id, user_id, level, title, content, read_status, created_at, read_at
    </sql>

    <select id="countUnread" resultType="long">
        SELECT COUNT(*) FROM notification
        WHERE user_id = #{userId} AND read_status = 'UNREAD'
    </select>

    <select id="selectByUser" resultType="com.baiflow.transfer.entity.Notification">
        SELECT <include refid="notifColumns"/>
        FROM notification
        WHERE user_id = #{userId}
        <if test="readStatus != null and readStatus != ''">
            AND read_status = #{readStatus}
        </if>
        ORDER BY created_at DESC
    </select>

</mapper>
