<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baiflow.transfer.mapper.TransferTaskMapper">

    <sql id="taskColumns">
        id, created_by, task_type, source_type, target_type,
        status, progress, error_message, created_at, updated_at
    </sql>

    <select id="selectByUser" resultType="com.baiflow.transfer.entity.TransferTask">
        SELECT <include refid="taskColumns"/>
        FROM transfer_task
        WHERE created_by = #{createdBy}
        <if test="taskType != null and taskType != ''">
            AND task_type = #{taskType}
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY created_at DESC
    </select>

</mapper>
